ConfigMaps passes key-value pairs to Pods as environment variables, sets of commands or volumes.

ConfigMaps can be created from literal values or just from REGULAR FILES like html/json/xml and DIRECTORIES.
